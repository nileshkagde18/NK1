trigger count2 on Child2__c (after insert, after delete)
{ 
/*    if (Trigger.isInsert && Trigger.isAfter)   //for insert
    {
         rolluptrigger.CountAdd2(Trigger.new);
    }
    
    if (Trigger.isDelete && Trigger.isAfter)    //for delete
    {
        rolluptrigger.CountDelete2(Trigger.old);
    }  */
}